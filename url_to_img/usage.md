Usage
python image_downloader.py


Enter the webpage URL

Enter a folder name (optional, default images)

The script downloads all images into that folder

✅ Features

Automatically creates a folder if it doesn’t exist

Converts relative URLs to absolute

Skips duplicate image URLs

Handles most image formats (.jpg, .png, .gif)